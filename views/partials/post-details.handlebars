<section class="col">
  <div class="card my-4 bg-dark">
    <a href="/post/{{id}}">
      <header class="ps-4 card-header bg-success">{{title}}</header>
      <p class="ps-4 card-body">{{body}}</p>
    </a>
  </div>
</section>